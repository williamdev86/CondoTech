<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title + ' | CondoTech' : 'CondoTech' %></title>
    <!-- Bootstrap 5 CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome para ícones -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <!-- Estilos customizados -->
    <style>
        .sidebar {
            min-height: calc(100vh - 56px);
            background-color: #343a40;
        }
        .sidebar a {
            color: rgba(255, 255, 255, 0.8);
            padding: 0.8rem 1rem;
            display: block;
            transition: all 0.3s;
        }
        .sidebar a:hover {
            color: #fff;
            background-color: rgba(255, 255, 255, 0.1);
        }
        .sidebar .active {
            color: #fff;
            background-color: #0d6efd;
        }
        .main-content {
            padding: 20px;
        }
        .card-dashboard {
            transition: all 0.3s;
        }
        .card-dashboard:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }
    </style>
    <%- typeof extraStyles !== 'undefined' ? extraStyles : '' %>
</head>
<body>
    <!-- Incluir o cabeçalho -->
    <%- include('../partials/header') %>
    
    <div class="container-fluid">
        <div class="row">
            <!-- Incluir a barra de navegação lateral -->
            <%- include('../partials/navbar') %>
            
            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 main-content">
                <!-- Mensagens de alerta -->
                <% if (typeof messages !== 'undefined' && messages.success) { %>
                    <div class="alert alert-success alert-dismissible fade show mt-3" role="alert">
                        <%= messages.success %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                <% } %>
                
                <% if (typeof messages !== 'undefined' && messages.error) { %>
                    <div class="alert alert-danger alert-dismissible fade show mt-3" role="alert">
                        <%= messages.error %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                <% } %>
                
                <!-- Conteúdo da página - isso é preenchido automaticamente pelo express-ejs-layouts -->
                <%- body %>
            </main>
        </div>
    </div>
    
    <!-- Incluir o rodapé -->
    <%- include('../partials/footer') %>
    
    <!-- Bootstrap 5 JS Bundle com Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- jQuery (para funcionalidades AJAX) -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    
    <!-- Scripts adicionais podem ser injetados aqui -->
    <%- typeof extraScripts !== 'undefined' ? extraScripts : '' %>
</body>
</html>